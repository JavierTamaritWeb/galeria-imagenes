{
  "name": "galeria-imagenes",
  "version": "1.1.2",
  "description": "Galería de imágenes CSS-only con cubos 3D — HTML + SCSS (BEM) + Gulp 5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "gulp",
    "build": "gulp build",
    "clean": "gulp clean"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "gulp": "^5.0.0",
    "gulp-sass": "^6.0.1",
    "sass": "^1.89.2"
  }
}
